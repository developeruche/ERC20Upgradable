{
  "name": "erc20upgradable",
  "version": "1.0.0",
  "description": "this is a hardhat project for an upgradable ERC20 token implementing the UUPS pattern",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "developeruche",
  "license": "MIT",
  "dependencies": {
    "hardhat": "^2.11.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0"
  }
}
